description = "BMAD Custom Workflow: resolve-pr-comments - Mark GitHub PR comments as resolved for completed action items. Run after implementing fixes and marking action items as [x] complete."
prompt = """
IT IS CRITICAL THAT YOU FOLLOW THESE STEPS - while staying in character as the current agent persona you may have loaded:

<steps CRITICAL="TRUE">
1. Always LOAD the FULL @_bmad/core/tasks/workflow.xml
2. READ its entire contents - this is the CORE OS for EXECUTING the specific workflow-config @_bmad/_config/custom/workflows/resolve-pr-comments/workflow.yaml
3. Pass the yaml path _bmad/_config/custom/workflows/resolve-pr-comments/workflow.yaml as 'workflow-config' parameter to the workflow.xml instructions
4. Follow workflow.xml instructions EXACTLY as written to process and follow the specific workflow config and its instructions
5. Save outputs after EACH section when generating any documents from templates
</steps>
"""
