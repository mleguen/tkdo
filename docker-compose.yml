services:
  composer:
    build: docker/composer
    entrypoint: composer
    environment:
      XDG_CACHE_HOME: /opt/tkdo/.cache
    user: composer
    volumes:
      - .:/opt/tkdo
    working_dir: /opt/tkdo/api
